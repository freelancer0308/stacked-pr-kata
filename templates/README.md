# Kata Search Adapter

This fictional adapter demonstrates the documentation layer of a stacked pull request.

```js
const result = await search('stacked pull requests', config)
```

The adapter returns an empty result set and never calls an external service.
